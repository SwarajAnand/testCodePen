{
    "routes": [{ "src": "/[^.]+", "dest": "/", "status": 200 }],
    "build": {
        "env": {
            "CI": "false"
        },
        "npm": {
            "installCommand": "npm install --legacy-peer-deps"
        }
    }
}
